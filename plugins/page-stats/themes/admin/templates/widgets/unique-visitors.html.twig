<div class="mb-5 {{ conf.cols_unique_visitors_widget }}">
    <h5>
        {{ "PLUGIN_PAGE_STATS.UNIQUE_VISITORS"|t }}
    </h5>
    <div style="width: w-100">
        <canvas id="chart_ips"></canvas>
    </div>
</div>
<script>
    var ctx = document.getElementById("chart_ips").getContext("2d");
    var myChart = new Chart(ctx, {
        type: "bar",
        options: {
            plugins: {
                legend: {
                    display: false
                }
            },
            scales: {
                y: {
                    beginAtZero: true
                }
            }
        },
        data: {
            datasets: [
                {
                    label: '{{ "PLUGIN_PAGE_STATS.TABLE_UNIQUE_VISITORS"|t }}',
                    data: [{% for s in data %}
                            {
                                x: "{{ s.date }}",
                                y: {{ s.hits }}
                            },{% endfor %}]
                }
            ]
        }
    });
</script>
